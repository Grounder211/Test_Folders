apiVersion: v1
kind: Service
metadata:
  name: file-server
spec:
  type: NodePort
  selector:
    app: file-server
  ports:
    - protocol: TCP
      port: 80          # Service port exposed internally in cluster
      targetPort: 5000  # Container port
      nodePort: 30080   # External port on the node to expose
